console.log("Hello World");

var tables = [
    'empty',
    'empty',
    'empty',
    'empty',
    'empty',
    'empty',
    'filled'
];
//set up UI and rewrite every update
function buildTableView() {
    //grab table container element so we can work with it
    //create a tableContainer 
    for(let index = 0; index < table.length; index++){
        var tableCircle = document.createElement("div");
    }
    //set on the tableContainer to give it styling
    
    //Add to html
}

//changed empty table to filled
function setTableToFilled(tableNumber ) {
    tables[tableNumber] = "filled";

}